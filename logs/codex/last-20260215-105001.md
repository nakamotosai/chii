已完成，且均为**追加写入**（未重写）：

- `memory/learn/polymarket_/kb.md`：追加了 `Round 127` 增量沉淀，包含你要求的五个模块：`套利玩法/交易策略/风控/常见坑/合规与道德边界`，并在关键结论末尾标注了 `(Round 127 Source N)`。
- `memory/learn/polymarket_/questions.md`：追加了 `1111-1118` 的未解问题，按优先级标注 `P0/P1`，并带 `(Round 127 Source N)` 标注。

本轮 Telegram 摘要（12 条）：
1. Round 127 在限定输入下 `src-*.txt` 仍为 0 命中，无法新增市场事实层结论。  
2. Round 127 在限定输入下 `src-*.url` 仍为 0 命中，无法建立“结论→URL”追溯链。  
3. 本轮执行层知识继续冻结，仅允许证据治理、流程阻断、风控与合规提醒。  
4. 套利玩法继续“空源禁执行”，禁止输出可实盘落地的多腿/对冲/抢跑等细节。  
5. 新增玩法“发布四锁”：`round_id=127`、`evidence_state=failed`、`tradability=none`、`execution_candidate=false` 缺一不可。  
6. 交易策略继续仅限“证据链修复+流程治理”，不允许收益目标、建平仓条件、参数阈值等执行语义。  
7. 新增“URL 零命中强制只读”：`src_url_count=0` 时策略必须 `publish_scope=research_only`。  
8. 风控保持最高级：`src_txt_count=0 && src_url_count=0` 时默认 `evidence_state=failed` 与 `execution_state=frozen`。  
9. 新增“失败态心跳校验”：每轮必须落地关键状态字段并做失配熔断与审计留痕。  
10. 新增“解冻前 URL 先决门槛”：`src_url_count` 未恢复非零前，禁止进入解冻评估。  
11. 常见坑聚焦三点：把轮次推进误判为恢复、URL 缺失仍复用历史参数模板、未记录失败态字段导致审计断链。  
12. 合规边界重申：仅提供合法合规研究与风控信息，不提供规避监管/地区限制/KYC-AML/平台规则的指导。