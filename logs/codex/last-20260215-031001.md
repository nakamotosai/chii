已完成，均为**追加写入**：

- `memory/learn/polymarket_/kb.md`：追加了 **Round 81 增量沉淀**（含“套利玩法/交易策略/风控/常见坑/合规与道德边界”全套结构），并且每条关键结论都带了 `(Round 81 Source N)` 标注。
- `memory/learn/polymarket_/questions.md`：追加了 **661-670** 共 10 条未解问题，按优先级（P0/P1）排序，均带 `(Round 81 Source N)` 标注。

本轮输入事实：`round-81` 下 `src-*.txt` 与 `src-*.url` 均为 0 命中，仅有 `search-*.json` 与 `urls.txt`（不在允许证据边界内）。

**Round 81 回传 Telegram 摘要（12条）**
1. Round 81 仍是空源轮次：`src-*.txt=0`、`src-*.url=0`。  
2. 连续空源推进到第 72 轮，继续判定为“证据治理增量”，非“交易知识增量”。  
3. 本轮继续禁止输出任何可执行套利参数与下单细节。  
4. 新增“证据可用性心跳门禁”：每轮需先产出 `evidence_heartbeat`。  
5. `evidence_heartbeat` 至少覆盖 `txt_count/url_count/配对率`，缺字段即维持 `tradability=blocked`。  
6. 新增“空源超时升级”思路：超过恢复窗口仍双空时，只允许维护注释，不允许新增可执行草案。  
7. 策略侧继续“仅排障输出”，禁止收益判断和参数迭代。  
8. 新增“策略发布前心跳核验”：无心跳编号与校验结果，不得出现阈值/参数/优先级。  
9. 风控维持最高级：未满足“非零正文+非零URL+一一配对+段落可定位”不得入库。  
10. 新增“心跳缺失即告警升级”，防止静默漂移和假恢复。  
11. 常见坑强调：`search-*.json`、`urls.txt` 不能替代 `src-*.txt/.url` 证据链。  
12. 合规边界维持不变：仅提供合法研究与风控建议，不提供绕监管/KYC/地区限制的指导。