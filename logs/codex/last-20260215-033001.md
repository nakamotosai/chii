已完成本轮追加写入（仅追加，未重写）：

- `memory/learn/polymarket_/kb.md`：已追加 **Round 83 增量沉淀**（含 套利玩法 / 交易策略 / 风控 / 常见坑 / 合规与道德边界），并为关键结论添加 `(Round 83 Source N)` 标注。  
- `memory/learn/polymarket_/questions.md`：已追加 **未解问题 681-690**，按优先级（P0/P1）排列，并带 `(Round 83 Source N)` 标注。  

本轮用于回传 Telegram 的摘要（12 条）：

1. Round 83 限定输入下，`src-*.txt` 仍为 0 命中，未产生可提炼正文证据。  
2. Round 83 限定输入下，`src-*.url` 仍为 0 命中，URL 追溯链不可构建。  
3. 连续空源轮次从 73 提升到 **74**，本轮继续是“证据治理增量”而非“交易知识增量”。  
4. 套利玩法层继续冻结：禁止补写跨市场对冲、YES+NO 合成、负风险组合、事件篮子配平等可执行步骤。  
5. 新增“账龄跨阈值冻结”：74 轮空源下，玩法默认 `tradability=blocked` 且 `publish_scope=research_only`。  
6. 新增“恢复闸门最小化发布”：恢复前仅允许维护验收模板与失败样例，不允许升级为可执行玩法草案。  
7. 交易策略层继续“仅排障输出”：只能写抓取/映射/落盘校验，不能写收益判断与参数迭代。  
8. 新增“最小化发布白名单”：空源轮次只允许 `evidence_status/risk_level/repair_progress` 三类字段。  
9. 新增“恢复前双侧必达”：仅恢复 `txt` 或仅恢复 `url` 都视为假恢复，保持 `execution_state=frozen`。  
10. 风控维持最高等级：证据恢复前，交易侧主张默认高不确定并拒绝入库。  
11. 常见坑强调：文档输出不等于证据恢复；示例参数/假设收益会形成隐性交易指令风险。  
12. 合规边界维持不变：仅提供合法研究与风控建议，不提供规避监管/KYC/地区限制的指导；恢复后先做条款、准入、数据许可、披露、税务与反洗钱复核。