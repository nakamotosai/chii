已完成本轮写入（仅追加、未重写）：

- `memory/learn/polymarket_/kb.md`：已追加 `Round 23 增量沉淀` 分节，覆盖“套利玩法/交易策略/风控/常见坑/合规与道德边界”，并在关键结论末尾标注 `(Round 23 Source N)`。
- `memory/learn/polymarket_/questions.md`：已续号追加 `114-121` 共 8 条未解问题，按优先级（`P0/P1`）组织，并附来源标注。
- 严格使用了本轮可用材料：`round-23` 目录中的空文件事实（`src-*` 缺失、`search-*.json` 与 `urls.txt` 均 0 字节）。

本轮摘要（用于 Telegram，14 条）：
1. Round 23 仍未生成任何 `src-*.txt` 与 `src-*.url`，最小证据输入单元缺失。  
2. `round-23` 仅有 `search-1..5.json` + `urls.txt`，且全部 0 字节。  
3. 本轮结论性质为“来源完全不可用”，不是“来源较少”。  
4. 已定义为第 14 个连续空源轮次。  
5. 套利玩法层继续冻结，不新增跨平台对冲/YES+NO 合成/负风险组合/事件篮子配平细节。  
6. 交易策略层继续执行“仅排障输出”，禁止参数评审与策略升级。  
7. 参数冻结继续生效：最小价差、深度门槛、滑点、撤单超时、重试、资金分层均不变。  
8. 风控状态维持红色：证据恢复前只允许链路排障与输入契约修复。  
9. 入库门禁继续要求“至少 1 对非零 `src-*.txt/.url` + URL 可追溯 + 段落可定位”。  
10. 常见坑：把轮次推进误当知识推进。  
11. 常见坑：把 `search-*.json`/`urls.txt` 文件存在误当可学习证据。  
12. 常见坑：在空源轮次补写执行参数会制造伪增量并污染回测。  
13. 合规边界重申：仅做合法合规研究与风险提醒，不给规避监管/规则/KYC 的指导。  
14. `questions.md` 已新增 8 条补洞问题（114-121），聚焦根因定位、输入契约硬校验、自动解锁条件与证据恢复后的玩法/策略/风控复核。