结论：最近几小时的“定时任务报错”主要是**通知链路噪音**，不是学习任务本体失败。

1. 主要根因（Telegram 重复报错）
- 有一个历史任务 `codex-20260214-150115` 被标记为 `done`，但 `chatId` 是无效值 `"0"`：`logs/codex/jobs/codex-20260214-150115.json:5`
- 该任务的 `lastMsg` 文件是空文件（0 字节），但旧 runner 只判断“文件存在”就算 `done`：`logs/codex/jobs/codex-20260214-150115.sh:19`、`logs/codex/jobs/codex-20260214-150115.sh:20`
- 通知器会扫描所有 `done && notified=false`，且 `chatId="0"` 会被当成非空继续发送：`scripts/codex_job_notifier.sh:28`、`scripts/codex_job_notifier.sh:56`
- 所以每轮都会报：`Bad Request: chat not found` + `SEND_FAILED: codex-20260214-150115`（例如 `logs/codex/jobs/codex-20260215-010001.log:2`、`logs/codex/jobs/codex-20260215-010001.log:3`）

2. 次要根因（Gateway 405）
- 每个任务结束都会调用 `gateway_wake.sh`：`scripts/codex_dispatch.sh:74`
- `gateway_wake.sh` 固定 `POST /api/cron/wake`：`scripts/gateway_wake.sh:30`
- 当前接口返回 405（方法不允许），所以每轮日志第一行都出现：`curl: (22) ... 405`（如 `logs/codex/jobs/codex-20260215-010001.log:1`）

3. 最近几小时情况
- 最近 3 小时内有 19 个 job log 出现同一组三连报错（405 / chat not found / SEND_FAILED）。
- 但任务 JSON 多数是 `status: "done"`（如 `logs/codex/jobs/codex-20260215-010001.json:4`），说明任务执行本体基本完成，报错集中在回调与通知阶段。

如果你要，我可以下一步直接帮你做“最小修复”：
1. 把坏任务 `codex-20260214-150115` 标记为已处理（避免继续重试）。
2. 给通知器加 `chatId` 有效性校验（过滤 `0`/非法值）。
3. 校正 `gateway_wake` 的请求方法或临时关闭该唤醒调用。