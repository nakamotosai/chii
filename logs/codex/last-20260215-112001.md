已完成，且均为“仅追加”写入：

- `memory/learn/polymarket_/kb.md:4207` 追加了 **Round 130 增量沉淀**（含：套利玩法/交易策略/风控/常见坑/合规与道德边界），每个关键结论已带 `(Round 130 Source N)` 标注。
- `memory/learn/polymarket_/questions.md:1149` 追加了 **10 条未解问题**（编号 `1139-1148`，按 `P0/P1` 优先级），每条已带 `(Round 130 Source N)` 标注。

本轮 Telegram 摘要（12 条）：
1. Round 130 在限定输入下，`src-*.txt` 0 命中，无法形成正文证据。  
2. Round 130 在限定输入下，`src-*.url` 0 命中，无法形成 URL 追溯链。  
3. 因双侧证据同时缺失，本轮只允许沉淀流程治理与风险边界，不允许沉淀可执行交易细节。  
4. 套利玩法层继续“空证据禁执行”，禁止输出可直接下单的玩法描述。  
5. 新增“玩法发布五键门禁”：`round_id=130`、`evidence_state=failed`、`tradability=none`、`execution_candidate=false`、`publish_scope=research_only`。  
6. 交易策略层继续冻结执行参数，禁止收益目标、建平仓触发、阈值、时序等内容。  
7. 新增“URL 先决解锁”：只有正文与 URL 双侧恢复且完成映射，策略才可进候选态。  
8. 风控层维持最高等级：双空即 `evidence_state=failed` + `execution_state=frozen`。  
9. 新增“证据指纹”建议：每轮固定记录 `round_id/src_txt_count/src_url_count/evidence_state/execution_state/check_time`。  
10. 常见坑：把“目录存在”误判为“证据可用”；在 URL 缺失时提前写参数；未绑定 round_id 导致模板污染。  
11. 合规边界：仅可输出合法研究与风控信息，不提供绕监管、绕 KYC/AML、绕平台规则的指导。  
12. questions 已新增 10 条高优先级问题，核心聚焦根因定位、URL 原子提交、双空门禁工程化、合规放行链路。