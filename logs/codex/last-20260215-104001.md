已完成，且均为**追加写入**（未重写）：

- 已追加到 `memory/learn/polymarket_/kb.md`：`Round 126 增量沉淀`（含：套利玩法 / 交易策略 / 风控 / 常见坑 / 合规与道德边界 / 输入边界复核）。
- 已追加到 `memory/learn/polymarket_/questions.md`：`1101-1110`（按优先级 `P0/P1`）。
- 关键结论均已带来源标注：`(Round 126 Source N)`。
- 本轮依据限定输入结论为：`src-*.txt` 与 `src-*.url` 均无文件实体（双空），执行层知识继续冻结。

本轮摘要（Telegram 版，15 条）：
1. Round 126 在限定输入下，`src-*.txt` 为 0 命中，事实层市场知识无新增。  
2. Round 126 在限定输入下，`src-*.url` 为 0 命中，结论无法建立 URL 追溯链。  
3. 本轮继续执行“空源禁执行”，不输出任何可实盘套利参数。  
4. 套利玩法层新增“代际锁二次校验”：必须绑定 `round_id=126` + `evidence_state=failed`。  
5. 交易策略层新增“URL 缺失硬回退”：强制 `publish_scope=research_only`。  
6. 风控维持最高等级：`src_txt_count=0 && src_url_count=0` -> `evidence_state=failed`、`execution_state=frozen`。  
7. 新增“解冻前一致性复核单”要求：正文/URL/映射/定位/签收/连续稳定缺一不可。  
8. 新增“失败态发布拦截”：冻结态与发布态失配时必须自动熔断。  
9. 常见坑：把“目录新增”误判为“证据恢复”。  
10. 常见坑：URL 缺失时复用历史结论，导致代际污染和不可审计。  
11. 常见坑：保留收益率/仓位/阈值占位字段，被下游误读为执行建议。  
12. 合规边界：仅提供研究治理与风险控制，不提供规避监管/KYC/AML/平台规则指导。  
13. 在证据链恢复前，明确拒绝可执行套利路径输出。  
14. 即使后续恢复证据，也需先完成条款、准入、数据许可、税务、AML、披露核验。  
15. 已追加未解问题 10 条（1101-1110），重点覆盖根因定位、URL 原子提交、双空门禁工程化、合规放行链路。