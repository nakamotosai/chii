已完成本轮写入（均为**追加**）：

- 已追加到 `memory/learn/polymarket_/kb.md`：`Round 131 增量沉淀`
- 已追加到 `memory/learn/polymarket_/questions.md`：`1149-1158`（按优先级）
- 所有关键结论与问题都带了来源标注：`(Round 131 Source N)`
- 结构已覆盖：套利玩法 / 交易策略 / 风控 / 常见坑 / 合规与道德边界（仅风险提醒，无非法指导）

本轮摘要（用于 Telegram，14 条）：
1. Round 131 在限定输入下 `src-*.txt` 0 命中，事实层新增为 0。  
2. Round 131 在限定输入下 `src-*.url` 0 命中，结论到 URL 追溯链不可建立。  
3. 本轮仅能沉淀“证据治理+流程阻断+风险与合规边界”，执行层继续冻结。  
4. 套利玩法继续禁执行：不输出跨市场对冲、多腿配平、负风险拼接等可执行细节。  
5. 新增玩法约束：空源轮次玩法草案只能进入只读快照区，不得进候选/评审/外发。  
6. 交易策略继续只允许流程治理，不允许收益目标、参数阈值、时序触发等执行语义。  
7. 新增策略约束：URL 缺失状态下，价格/仓位/阈值/收益率字段发布前必须拒绝入库。  
8. 风控维持最高：`src_txt_count=0 && src_url_count=0` 强制 `evidence_state=failed` 与 `execution_state=frozen`。  
9. 新增风控建议：固定记录六字段（round_id、txt/url 计数、evidence_state、execution_state、check_time），失配即熔断。  
10. 解冻前置条件再次强调：正文非空 + URL 非空 + 一一映射 + 段落定位 + 责任签收，缺一不可。  
11. 常见坑：把“轮次推进”误判为“证据恢复”。  
12. 常见坑：URL 缺失时先写参数草稿，导致不可追溯文本被误读为可执行建议。  
13. 常见坑：冻结态关键字段缺失，导致审计链断裂。  
14. 合规边界：仅输出合法研究与风控信息，不提供任何规避监管/KYC/AML/地区限制的指导。