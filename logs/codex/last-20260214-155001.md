已完成，按“只追加”更新了两个文件：

- `memory/learn/polymarket_/kb.md`：追加 **Round 13 增量沉淀**
- `memory/learn/polymarket_/questions.md`：追加 **Round 13 未解问题（P0/P1）**

说明：本轮 `round-13` 目录下可用文件均为 0 字节，且无 `src-*.txt/.url`，所以新增内容以“证据缺失下的流程修正、风控与边界约束”为主，未虚构交易参数。

**本轮摘要（Telegram 版，12 条）**
1. Round 13 原始来源不可用：`search-1..5.json` 与 `urls.txt` 全是 0 字节，且缺失 `src-*.txt/.url`。  
2. 因无可读证据，本轮不能新增任何实盘套利参数或行情结论。  
3. 新增“玩法停机条件”：缺少可读 `src-*.txt/.url` 时，禁止新增/修正套利玩法细节。  
4. 既有玩法在空源轮次只能“状态标注”，不能“阈值改写”。  
5. 新增策略“证据链分级”：`E0` 无源、`E1` 有 URL 无正文、`E2` 正文不可复核、`E3` 正文+URL 可复核。  
6. 仅 `E3` 允许写入新策略参数；Round 13 被归类为 `E0`。  
7. Round 13 明确禁止输出新执行参数（价差阈值、深度、滑点、撤单超时、重试等）。  
8. 风控新增：连续 ≥3 轮 `E0` 视为研究风控事件，应优先排障并暂停策略增量发布。  
9. 写 KB 前新增硬要求：必须先记录来源完整性检查（文件名、字节数、是否成对）。  
10. 常见坑修正：目录存在不等于来源可用，0 字节文件不能作为证据。  
11. 常见坑修正：连续空源却“模板化增量”会制造伪进展并污染知识库。  
12. 合规边界重申：仅做合法风险提醒与流程修复，不提供规避监管/绕过平台规则/KYC 的指导。