# OpenClaw 提示词升级决策（2026-02-07）

## 结论
- **不升级 OpenClaw 核心提示词引擎**（system-prompt.ts 等源码不动）。
- 只做 **低风险的“可逆”增强**：记录、核查、监控；不改核心逻辑。

## 为什么不升级核心引擎
1. 当前系统稳定、符合主人的使用习惯；升级风险 > 收益。
2. 核心 prompt 的模块化已满足需求；贸然动会破坏“像你”的语气。
3. 已完成全量备份，后续如需升级可分批验证再做。

## 本次实际执行（低风险）
1. **建立升级决策文档**（本文件）
2. **增加提示词健康检查脚本**（见 `scripts/prompt_healthcheck.sh`）：
   - 只检查关键文件是否存在、大小/时间、提示词模式提示是否缺失
   - 不触碰代码、不修改配置

## 后续如需升级的可选项（暂不执行）
- 模块化提示词可视化（自动解析系统提示词构建函数）
- PromptMode 管理器（按 agent/session 做 full/minimal 统一配置）
- Core-file 模板化（SOUL/AGENTS/TOOLS/USER 一键套用）

## 回滚策略
- 如需回滚任何后续变更，直接使用备份：
  `/home/ubuntu/workspace-backup-2026-02-07.tar.gz`
